# HANDOFF

**Last session:** 2026-08-09 (Session 6 — `03_Video_Surveillance/`, the full module)
**State:** **Phases 1–7 complete.** Four modules are finished to the full shape — 01, 03, 32, 35 —
and the repo has zero dangling solution links except the module 30 capstone reference solution.
Module 03 is the largest (~76k words) and is the reference for how an *application* module relates
to a *derivation* module. Phase 8 (`04_Access_Control/`) is next.

---

## Repository

- **Remote:** `git@github.com:cs-keni/physical-security.git`
- **Default branch:** `main`
- **Workflow:** feature branch per work item (e.g. `module/35-doors-hardware`), then merge to
  `main`. Never commit real project data — `.gitignore` guards `*_CONFIDENTIAL*` / `*_CLIENT*`
  but the real control is discipline.
- **Commit convention:** imperative subject ≤72 chars stating *what* and *why*. Update
  `COURSE_PROGRESS.md`, `PHASES.md`, and `docs/ENGINEERING_LOG.md` **in the same commit** as
  the content they describe.

---

## Architecture

```
physical-security/
├── README.md                  entry point, repo map, ground rules
├── COURSE_PROGRESS.md         ← BUILD STATUS. Read first when resuming.
├── PHASES.md                  build plan
├── docs/                      agent context (this dir)
├── 00_Roadmap/                how to study, 12-month plan, skills matrix, gap analysis
├── 01–38_<Module>/            instructional content; solutions in _solutions/
├── 25_Quizzes/_answer_keys/   isolated
├── 27_Labs/_solutions/        isolated
├── 28_Calculators/psec/       ← WORKING CODE. 68 tests.
├── 16_Automation/data_model/  ← WORKING CODE. Device schema + validator.
└── 31_References/             source_index.md — the verification record
```

**Two kinds of artifact, different rules:**
- **Content** (Markdown) — follows the 20-part module template in `AI_CONTEXT.md`
- **Code** (Python) — standard library only, tested, documented assumptions

---

## Key architectural decisions

| Decision | Rationale |
|---|---|
| **Solutions isolated in `_solutions/` / `_answer_keys/`** | The learner explicitly asked not to be able to spoil exercises by scrolling. Never inline an answer. |
| **No placeholder files** | "Does the file exist?" must be a reliable completeness signal. Unwritten = empty dir + a `COURSE_PROGRESS.md` row. |
| **Calculators are stdlib-only** | Must run on a locked-down work laptop with no software request. |
| **Tests encode the hand calculations** | `32_Engineering_Math/` lessons are written *from* the test file. If a test fails after a formula change, redo the hand calc — don't change the test. Vindicated in Session 4: hand-checking units found two real `psec` defects the passing suite had missed. |
| **Device data model is the single source of truth** | Drawings, schedules, IP plan, cable schedule, commissioning tracker are all projections. This is the learner's differentiator. |
| **Validator reports, never mutates** | "Automate the repetitive work, never the engineering judgment." A tool that silently fixes data will silently break it. |
| **Phase-aware validation** | A validator that cries wolf gets ignored, which is worse than none. |
| **Bluebeam automation via exports, not scripting** | Scripting is Max-only; the user has Complete. Verified against official docs. |
| **Doors/hardware and electrical split into their own modules (35, 34)** | They're the top two junior knowledge gaps and deserve dedicated depth. |
| **Engineering math (32) separate from calculators (28)** | Derivations are pedagogically distinct from tools; separation prevents using a calculator you don't understand. |
| **`_solutions/` written in the same commit as the lessons that link to them** | Module 01 shipped lessons whose exercise links dangled for four sessions. Modules 35 and 32 did not repeat it, and module 01's debt was closed in Session 5. **The repo now has zero dangling solution links.** Non-negotiable for every module from here. |
| **Every numeric value in a code-backed module is produced by running the code** | Adopted in module 35, made a hard rule in module 32. Never hand-write a number into a lesson whose subject `psec` implements. It keeps prose and tests from drifting and it is what surfaced the two defects below. |
| **Worked numeric examples are computed with `psec`, not written by hand into prose** | Module 35 lessons 03 and 06 and their solutions were generated by running `psec.power` and transcribing the output. The numbers are reproducible and a formula change will surface as a mismatch rather than as stale prose. |
| **A complete module has a fixed shape** | Overview · lessons · `_solutions/` for every lesson · a capstone field exercise with a reference set · a quiz with an isolated answer key · a validated flashcard deck. **Module 35 is the reference implementation.** Match it. |
| **Worked examples chain across lessons within a module** | Module 35 lesson 06's voltage-drop example is deliberately the same opening as lesson 03's, and the answer changes once the transfer segment is added. Continuity teaches better than a fresh contrived example, and it makes the omission memorable. Module 03 extends this: one 31-camera site (Meridian) carries lessons 06–09. |
| **An application module never re-derives its derivation module's math** | Established in module 03 against module 32. The overview opens with a **division-of-labour table** — *"why is `W = D·w/f` true?"* → module 32; *"should this camera be looking there at all?"* → module 03. Every formula links out and states only the result. Without it, module 03 would have duplicated ~40% of module 32. **Apply the same pattern for module 04 against module 35.** |
| **A capstone site is chosen to break its own module's instincts** | Module 01's library inverts hardening; module 32's 3PL centre fails four times before it works; module 03's park-and-ride garage defeats three trained instincts at once (identify-at-the-incident, light-is-always-binding, and where the chokepoint is). The reference solution names the traps explicitly rather than quietly avoiding them. |
| **When the arithmetic contradicts the planned narrative, the arithmetic wins and becomes the lesson** | Three times in module 03 a computed result inverted the expected answer, and each became the teaching point: geometry passing while the image fails; an 8 MP upgrade needing the same camera count; a 99% analytics improvement still leaving 0.905% precision. **Never smooth a surprising result back into the expected story.** |

---

## Content conventions that must not drift

- Tag system: `[CODE]` `[STANDARD]` `[GUIDELINE]` `[PRACTICE]` `[MFR]` `[VERIFY]`
- Icons: 🧮 calculation · 🔧 lab · ⚠️ mistake · 🧠 senior insight
- Every module: Junior vs. Senior sections, Common Mistakes, Retrieval Check, References
- Every code/standard numeric claim: tagged `[VERIFY]`
- ASCII diagrams only (must render in a plain text editor)

---

## Outstanding risks and debts

1. **ASIS certification data unverified** — asisonline.org 403s automated fetch. Domain names
   and weightings in `31_References/source_index.md` are flagged low-confidence. **A human
   must download the official Certification Handbook.** Do not build the APP/PSP tracks on the
   provisional figures.
2. ~~**`32_Engineering_Math/` lags the calculators.**~~ **Resolved (Session 4).**
3. ~~**Module 01 embedded exercises lack solution files.**~~ **Resolved (Session 5.)** Note the
   count in every doc said 4; there were **6**.
4. **Capstone has no dimensioned site geometry** — prose description only. Still the only broken
   link in the repo (`30_Capstones/data_center_campus/` → `_reference_solution/`).
5. **Four provisional APP/PSP mapping tables** now exist (`01_Foundations/`,
   `03_Video_Surveillance/`, `32_Engineering_Math/`, `35_Doors_and_Hardware/` overviews). All need
   correcting when the ASIS block clears.
6. **`psec` has no depth-of-field or motion-blur functions, and module 03 needs both routinely.**
   DOF (hyperfocal, near/far limits) and motion blur (`smear_px = speed × exposure × PPF`) are
   geometric, testable, and belong in `psec.optics`. **Deliberately not added** — this repo's
   architecture is that module 32 derives what module 28 implements, so adding them properly means
   writing the derivations in module 32 first, reopening a module marked complete. Until then the
   arithmetic is shown in full in module 03 lessons 01, 02, and 03 so it can be hand-checked.

---

## Verification commands

```bash
python3 28_Calculators/tests/test_psec.py     # 68 tests → OK
python3 28_Calculators/demo.py                 # 8 worked examples
python3 16_Automation/data_model/validate.py \
        16_Automation/sample_data/devices_flawed.csv CD   # 25 errors, all genuine
python3 -c "import csv; print(len(list(csv.DictReader(open('26_Flashcards/01_foundations.csv')))))"
python3 -c "import csv; print(len(list(csv.DictReader(open('26_Flashcards/35_doors_hardware.csv')))))"
```

**Link check** — run after adding any module, since cross-references between modules are easy to
get wrong and nothing else catches them:

```bash
python3 - <<'EOF'
import re, os
root = os.getcwd()
pat = re.compile(r'\[[^\]]*\]\(([^)\s#]+)')
broken = []
for d, _, fs in os.walk(root):
    if '.git' in d:
        continue
    for f in fs:
        if not f.endswith('.md'):
            continue
        p = os.path.join(d, f)
        for m in pat.finditer(open(p, encoding='utf-8').read()):
            link = m.group(1)
            if link.startswith(('http', 'mailto')):
                continue
            t = os.path.normpath(os.path.join(d, link))
            if not os.path.exists(t):
                broken.append((os.path.relpath(p, root), link))
print(f"{len(broken)} broken links")
for f, l in broken:
    print(f"  {f} -> {l}")
EOF
```

Note: this reports every unwritten module referenced by a cross-link. Treat hits in *written*
modules as errors; hits pointing at directories listed as pending in `COURSE_PROGRESS.md` are
expected.

---

## Next work item

See the bottom of [`../COURSE_PROGRESS.md`](../COURSE_PROGRESS.md).
Short version: **`04_Access_Control/` lessons 01–11.** Module 35 lessons 03, 04, and 06 hand off to
it directly — offline controller behaviour, REX strategy, and reader-in/reader-out are raised there
and resolved here. Module 03 lesson 08 forward-references it for PACS/video integration.

**Apply module 03's division-of-labour convention:** module 04 *applies* what module 35 *derives*
about doors and hardware. Open the overview with a table stating which question is answered where,
then link rather than restate. That convention is what stopped module 03 duplicating ~40% of
module 32 across 76k words.

Match the depth and voice of `01_Foundations/03_functional_chain.md`,
`35_Doors_and_Hardware/03_locking_hardware_families.md`,
`32_Engineering_Math/06_voltage_drop.md`, and
`27_Labs/_solutions/project_01_reference.md` — those are the quality bar.

**Match the shape of Modules 01, 32, and 35** — see the architectural decisions table above. In
particular: **write `_solutions/` in the same commit as the lessons that link to them**, and
**never hand-write a number into a lesson whose subject `psec` implements.**

---

## Session 4 carry-overs

**Two `psec` defects found and fixed while writing module 32.** Test count **66 → 68**. Both were
found by working units by hand, not by the test suite.

1. **`video.stream_gb_per_day(decimal_gb=False)` divided decimal megabytes by 1024.** A bitrate is
   decimal, so those megabytes are 10⁶ bytes; converting to GiB divides by 2³⁰/10⁶ = 1073.741824.
   The bug reported the decimal/binary gap at TB scale as 4.86% against a true 9.95% — the exact
   error the function's own docstring warned about. It survived because the only test on that path
   asserted `binary < decimal`, which holds either way. Fixed with named constants
   (`MB_TO_GIB_BINARY` and siblings) plus `test_binary_units_are_true_gibibytes` and
   `test_decimal_binary_gap_at_tb_scale_is_about_ten_percent`, the latter asserting against
   `2**40/1e12` — a value from the definition rather than from a run.
2. **`pps.compare_interventions` docstring said "three levers" and returns four.** Fixed, with the
   module-32 cross-reference added.

**The generalisable lesson:** a green suite is not evidence the units are right. An assertion of
the form `a < b` passes for the wrong reason more often than it fails for the right one. Where a
figure has a *known* correct value derivable from a definition, assert against the definition.

**Module 32's lesson filenames are pinned by source.** `optics.py`, `video.py`, and `power.py`
docstrings name exact paths (`01_camera_fov.md`, `02_pixel_density.md`, `03_bandwidth.md`,
`04_storage.md`, `05_poe.md`, `06_voltage_drop.md`, `07_battery_ups.md`). Grep for
`32_Engineering_Math` across the repo before renaming anything in that directory.

**Module 32 deviated from PHASES.md's planned lesson list, deliberately.** Rack/port/capacity
planning was folded into `05_poe.md` (it *is* the `PoESwitch` checks) and lesson 08 became the
adversary path derivation, because `psec/pps.py` had substantial tested math with no derivation
anywhere. Rationale recorded in `PHASES.md` Phase 6.

---

## Session 5 carry-overs

**The repo's oldest debt is closed.** Module 01 shipped in Session 1 with 9 dangling links, before
the write-solutions-in-the-same-commit convention existed. All 6 exercise solution files are
written, plus `vocabulary.md`, `checklist_foundations.md`, and `exercises.md`.

**The count in the old docs was wrong.** Every doc said "4 missing solution files." There were
**6** — lessons 02 through 07 each carry an exercise set. Worth remembering as a general caution:
a debt recorded once and copied forward across four sessions was never re-counted against the
files. **Re-derive a tracked number before you act on it.**

**`exercises.md` was ambiguous scope and became the module capstone.** The overview promised
"practice + scenarios," but every lesson already carries its own exercises, so a separate file
risked either duplication or a second dangling link. It is now an index of the per-lesson sets
plus **the Ashford Public Library capstone** — one site through all 7 lessons, with a reference
solution. That also gave module 01 the capstone it lacked, matching modules 32 and 35.

**The capstone is a library on purpose.** Every worked example in module 01 is a warehouse, an
office, or a server room — buildings where hardening is culturally acceptable and the assets are
property. A library inverts both: its mission is unrestricted public access and its most valuable
asset is staff. Its adversary-path arithmetic produces a result none of the module's other
examples do — **detection cannot make the path timely at any achievable sensor placement**, and
the delay lever, usually the worst value, is decisively correct. If a future module needs a site
where the standard answers fail, reuse this shape.

**Stale link fixed:** `01_Foundations/03_functional_chain.md` pointed at
`28_Calculators/timely_detection.py`, superseded by `psec/pps.py` back in Phase 3 and never
updated. It now points at `psec/pps.py` and at `32_Engineering_Math/08_adversary_path.md`.

**One broken link remains repo-wide:** `30_Capstones/data_center_campus/00_BRIEF.md` →
`_reference_solution/`. It is `COURSE_PROGRESS.md` known issue 4 and is now the only solution debt
in the repo.
